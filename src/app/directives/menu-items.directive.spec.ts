import { MenuItemsDirective } from './menu-items.directive';

describe('MenuItemsDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuItemsDirective();
    expect(directive).toBeTruthy();
  });
});
